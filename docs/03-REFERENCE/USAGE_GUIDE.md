# Usage Guide

This folder is intentionally local-only and ignored by Git.

## Run the pipeline

Use the project root to run the main automation:

```bash
cd /Users/apple/Desktop/SalwynFolder/ai-job-hunter/ai-job-automation
TARGET_ROLE=software_developer_intern python3 main.py
```

## Local notes

- `.env` files are not pushed.
- `Docs/` is ignored.
- Markdown files in `Docs/` are for local reference only.
